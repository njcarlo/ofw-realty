// Notification service — in-app (Supabase Realtime), email (Resend), push (Expo FCM/APNs)

async function bootstrap() {
  console.log('Notification service starting on port 3006')
  // NestJS bootstrap will be wired in Task 16
}
bootstrap()
